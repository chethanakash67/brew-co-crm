---
trigger: always_on
---

Modify only the specified component.

Do not rewrite entire files.

Use existing tokens from /lib/design-system.ts.

Do not introduce new colors, spacing, or animations.

Preserve existing structure unless explicitly changed.